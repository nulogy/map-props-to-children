const assert = require('assert');
const mapPropsToChildren = require('../index.js');

describe('mapPropsToChildren', function () {
  it('has a test', function () {
    assert(false, 'mapPropsToChildren should have a test');
  });
});
